service: email-test
# Here we can declare which resource template
# Should be used in diffent selected types
types:
  # Serverless MIT provider
  serverless: serverless.yml

# Here we have the allowed templates to be inputted
template:
  body: String
  title: String
  to: String
  from: String
  attachment?: [String]

mock:
  service: my-test-service
  name: myTestService2
  args:
    - body: Hello from an Lambda function
      title: Hello from an body
      from: erik.rehn98@gmail.com
      to: erik.rehn98@gmail.com
